trigger:
- main

pool:
  vmImage: pool-selfhosted

steps:
- task: AzureKeyVault@2
  inputs:
    azureSubscription: 'sp-ado-kv-reader'   
    KeyVaultName: 'kv-prodtest-secrets'     
    SecretsFilter: 'cicd-pipeline-secret'
    RunAsPreJob: true

- script: |
    echo "Pipeline retrieved secret successfully"
